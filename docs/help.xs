(
"
    _flag_ if
        _code_
    endif

if _flag_ is non-zero _code_ is executed.

    _flag_ if
        _code1_
    else
        _code2_
    endif

if _flag_ is zero then _code2_ is executed.
"
[ "if" "else" "endif" ] doc
)

