[package]
name = "xeh"
version = "0.1.0"
authors = ["rivo <db05@bk.ru>"]
edition = "2018"

[features]
default = ["mmap"]
mmap = ["mapr"]

[dependencies]
rpds = "0.7"
rustyline = "6.1.2"
getopts = "0.2"
getrandom = { version = "0.2.2", features = ["js"] }
twoway = "0.2.1"

mapr = { version = "0.8", optional = true }
